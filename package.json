{
  "name": "finances",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run --maxWorkers=1 --coverage",
    "test:watch": "vitest --maxWorkers=1",
    "test:unit": "vitest run tests/unit --maxWorkers=1",
    "test:integration": "vitest run tests/integration --maxWorkers=1",
    "test:ci": "vitest run --maxWorkers=1 --coverage --reporter=default",
    "dev": "node backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "minio": "^8.0.7",
    "multer": "^2.1.1",
    "pg": "^8.19.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^11.7.3",
    "@vitest/coverage-v8": "^3.2.4",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  }
}
